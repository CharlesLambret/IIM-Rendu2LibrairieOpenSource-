name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Use Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '20'

    - name: Install Dependencies
      run: npm i

    - name: Run Linter
      run: npx eslint dateUtils.js

    - name: Run Tests
      run: npm test

    - name: Publish to NPM
      run: npm publish
      env:
        NODE_AUTH_TOKEN: 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
      if: github.ref == 'refs/heads/main'
